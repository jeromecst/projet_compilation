int mms = 667;

int x = 1;

int fact(int n) {
	if (n < 2) {
		// erreur sous cette ligne car a n'est pas déclaré
		return a;
	} else {
		return n * fact(n + -1);
	}
}

void main() {
	putchar(fact(mms));
}
